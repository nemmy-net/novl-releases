Run update.go in this folder to update index.json and the gzip directory.

File structure:
- index.json: Index of every platform, version, file, and file hash.
- gzip: Contains the gzipped version of every file. Each file name is the hash of an original file.

The remaining folders contain every version of their respective software/assets.